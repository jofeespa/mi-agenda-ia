name: mi_agenda_ia
description: "Agenda personal por voz: notas, tareas, recordatorios y calendario."
publish_to: "none"
version: 0.1.3+4

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: 7.4.0
  flutter_local_notifications: 22.3.0
  shared_preferences: 2.5.5
  timezone: 0.11.1
  flutter_timezone: 5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
